# retail_analytics_dashboard
A live, interactive analytics dashboard built on synthetic CPG/retail data.

To run it locally:

# 1. Install dependencies
pip install -r requirements.txt

# 2. Generate the data
python data/generate_data.py

# 3. Launch the app
streamlit run app.py
